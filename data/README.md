# Data Alias

`data/` is a compatibility alias for older scripts and docs.

Canonical locations now live under:

- `datasets/raw/`
- `datasets/processed/`
- `datasets/synthetic/`

If you are adding new documentation or new experiment procedures, use `datasets/...`.
